# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

name: jenkins-agent

image:
  repository: jenkins/agent:latest
  pullPolicy: IfNotPresent

externalAccess:
  enabled: false
storage:
  enabled: false
docker:
  enabled: false